# This was cloned from an Eclipse template for yasnippet.
# --
	<select id="${id}"
		parameterClass="${parameterClass}"
		resultMap="${resultMap}">
		SELECT *
		  FROM ${table}
		 WHERE ${where}
	</select>
	